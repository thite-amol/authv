<span class="brand-title mdl-layout-title" title="{{ config('app.name', 'Authv') }}">
  <img class="brand-logo" src="{{ asset(config('authv.logo_url.desktop')) }}" />
  <img class="brand-mobile-logo" src="{{ asset(config('authv.logo_url.mobile')) }}" />
</span>
